'''
for titulo in titulos:
    print(titulo.get_text(strip=True))

# conectando ao banco
conexao = sqlite3.connect("orbita.db")
cursor = conexao.cursor()

# inserindo cada titulo no banco
for t in titulos:
    nome_missao = t.get_text(strip=True)
    cursor.execute('''
        INSERT INTO lancamentos_espaciais (nome_missao)
        VALUES (?)
    ''', (nome_missao,))
    

conexao.commit()
conexao.close()

'''